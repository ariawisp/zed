/// UI host functions for rendering Redwood/GPUI from extensions.
use redwood:protocol.{redwood-frame}

interface ui {
    /// Create (or ensure) a UI panel with the given id and title.
    create-panel: func(panel-id: u64, title: string);

    /// Close a previously created panel.
    close-panel: func(panel-id: u64);

    /// Apply a batch of typed UI mutations to the panel.
    apply: func(panel-id: u64, frame: redwood-frame);
}
